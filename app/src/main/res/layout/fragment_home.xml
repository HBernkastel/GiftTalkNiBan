<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:layout_centerVertical="true"
            android:layout_margin="@dimen/margin_image_to_parent_2"
            android:id="@+id/home_top_classify_iv"
            android:src="@drawable/arrow_grey_down"
            android:layout_alignParentEnd="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <android.support.design.widget.TabLayout
            android:id="@+id/home_top_tl"
            android:layout_width="match_parent"
            android:layout_height="@dimen/tab_height"
            app:tabIndicatorColor="@color/actionBar"
            app:tabSelectedTextColor="@color/actionBar"
            app:tabIndicatorHeight="@dimen/indicator_height"
            app:tabMode="scrollable"
            app:tabMaxWidth="@dimen/tab_width"
            android:layout_toStartOf="@id/home_top_classify_iv"/>
    </RelativeLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/home_content_vp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />



</LinearLayout>
